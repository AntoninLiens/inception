UPDATE mysql.user SET Password = PASSWORD('$MARIADB_ROOT_PASSWORD') WHERE User = 'root';

CREATE DATABASE $MARIADB_DATABASE;
CREATE USER '$MARIADB_USER'@'127.0.0.1' IDENTIFIED by '$MARIADB_PASSWORD';
GRANT ALL PRIVILEGES ON $MARIADB_DATABASE.* TO $MARIADB_USER@'127.0.0.1';

FLUSH PRIVILEGES;
